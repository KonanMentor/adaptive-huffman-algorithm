unit Encoder;

interface

uses
  Classes;

type
  TEncoder = class
    public
      constructor Create(Source, Destination: TStream);
  end;

implementation

constructor TEncoder.Create(Source, Destination: TStream);
begin

end;

end.
